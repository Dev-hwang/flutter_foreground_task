package com.pravera.flutter_foreground_task.errors

class NotificationPermissionRequestCancelledException :
    Exception("The dialog was closed or the request was canceled during a runtime notification permission request.")
